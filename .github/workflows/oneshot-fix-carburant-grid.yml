name: One-shot fix carburant desktop

on:
  push:
    branches:
      - fix/align-prix-perso-desktop
    paths:
      - .github/workflows/oneshot-fix-carburant-grid.yml
      - .github/patch_fix_carburant_grid.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/align-prix-perso-desktop
      - name: Patch index.html
        run: python .github/patch_fix_carburant_grid.py
      - name: Commit
        run: |
          if git diff --quiet -- index.html; then
            echo "Aucun changement"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Corrige le layout carburant desktop"
          git push
